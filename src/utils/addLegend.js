import { positionType } from '../config';

console.log(positionType);
/**
 * parent: {}, // a d3 selection component
 * items:[{
 *   color: 'red',
 *   text: 'tim'
 * }],
 * position: 'upleft'
 */
const addLegend = (parent, { items, position }) => {

  // 1. get background width and height
  // 2. 
};
